import React from 'react';
import { Button } from '@mui/material';

export default function AddFeeScheduleButton() {
  return (
    <Button variant="contained" color="primary">Add Fee Schedule</Button>
  );
}