package lambda;

// what is lombdo expression
// lambda is anonymous function
// no name
// no modifier
// no return type


// benefits of lambda expression
// 1 reduce the line of codes
// sequential and parallel execution support by passing behavior as an argument in methods
// to call apis very effectively
// to write more readable, maintainable and concise code.



// important rules of lambda expression
// if the body of the lambda expression contain only one statement then curly braces are optional
// java compiler also inter the type of variable in arguments hence type is optional.


// Functional Interface
// if the interface contains only one abstract method then it is functional interface
// ex Runnable, callable, comparable etc.
// to call lambda we require functional interface
// lambda is used to implements functional interface in very simple and short manner

public class lambda {
}
